{% extends 'base.html.twig' %}

{% block title %}Modifier le plat{% endblock %}

{% block body %}
<div class="container mt-5">
    <h1 class="text-center mb-4">Modifier le plat</h1>

    <!-- Formulaire -->
    <div class="card shadow p-4">
        {{ include('saloon_menu/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    </div>

    <!-- Boutons supplémentaires -->
    <div class="mt-3 d-flex justify-content-between">
        <a href="{{ path('app_saloon_menu_index') }}" class="btn btn-info btn-lg text-white">
            Retour à la liste
        </a>
        {{ include('saloon_menu/_delete_form.html.twig') }}
    </div>
</div>
{% endblock %}
