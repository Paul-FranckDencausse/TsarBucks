<form method="post" action="{{ path('app_admin_saloons_delete', {'id': saloon.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet élément ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ saloon.id) }}">
    <button type="submit" class="btn btn-lg btn-danger">
        Supprimer
    </button>
</form>
