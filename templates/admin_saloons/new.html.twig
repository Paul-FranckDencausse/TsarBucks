{% extends 'base.html.twig' %}

{% block title %}Nouveau Salon{% endblock %}

{% block body %}
<div class="container mt-5">
    <h1 class="mb-4">Créer un nouveau salon</h1>

    <!-- Inclusion du formulaire -->
    <div class="card p-4 shadow-sm">
        {{ include('admin_saloons/_form.html.twig', {'attr': {'class': 'form'}}) }}
    </div>

    <!-- Lien de retour -->
    <div class="mt-3">
        <a href="{{ path('app_admin_saloons_index') }}" class="btn btn-info btn-lg">
            <i class="bi bi-arrow-left"></i> Retour à la liste
        </a>
    </div>
</div>
{% endblock %}
