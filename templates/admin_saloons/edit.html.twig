{% extends 'base.html.twig' %}

{% block title %}Modifier le Salon{% endblock %}

{% block body %}
<div class="container mt-5">
    <h1 class="mb-4">Modifier le Salon</h1>

    <!-- Formulaire d'édition -->
    <div class="card p-4 shadow">
        {{ include('admin_saloons/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    </div>

    <!-- Actions supplémentaires -->
    <div class="mt-3 d-flex justify-content-between">
        <a href="{{ path('app_admin_saloons_index') }}" class="btn btn-info btn-lg">
            <i class="bi bi-arrow-left"></i> Retour à la liste
        </a>
        {{ include('admin_saloons/_delete_form.html.twig', {'attr': {'class': 'd-inline'}}) }}
    </div>
</div>
{% endblock %}
