{% extends 'base.html.twig' %}

{% block title %}Modifier le Contact{% endblock %}

{% block body %}
<div class="container mt-5">
    <h1 class="mb-4">Modifier le Contact</h1>

    <div class="card p-4 shadow-sm mb-4">
        {{ include('admin_contact/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    </div>

    <div class="d-flex justify-content-between mt-3">
        <a href="{{ path('app_admin_contact_index') }}" class="btn btn-primary">
            <i class="bi bi-arrow-left"></i> Retour à la liste
        </a>

        {{ include('admin_contact/_delete_form.html.twig') }}
    </div>
</div>
{% endblock %}
