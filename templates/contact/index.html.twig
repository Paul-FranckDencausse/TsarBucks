{% extends 'base.html.twig' %}

{% block title %}Contact - Tsarbucks{% endblock %}

{% block body %}
<section class="py-5 bg-light">
    <div class="container my-5">
        <h1 class="text-center">Contactez-nous</h1>

        {% if form is defined %}
            {{ form_start(form) }}
            <div class="mb-3">
                {{ form_label(form.salon, 'Salon', {'label_attr': {'class': 'form-label'}}) }}
                {{ form_widget(form.salon, {'attr': {'class': 'form-control'}}) }}
                {{ form_errors(form.salon) }}
                </div>

            <div class="mb-3">
                {{ form_label(form.email, 'Email', {'label_attr': {'class': 'form-label'}}) }}
                {{ form_widget(form.email, {'attr': {'class': 'form-control'}}) }}
                {{ form_errors(form.email) }}
            </div>

            <div class="mb-3">
                {{ form_label(form.subject, 'Sujet', {'label_attr': {'class': 'form-label'}}) }}
                {{ form_widget(form.subject, {'attr': {'class': 'form-control'}}) }}
                {{ form_errors(form.subject) }}
            </div>

            <div class="mb-3">
                {{ form_label(form.message, 'Message', {'label_attr': {'class': 'form-label'}}) }}
                {{ form_widget(form.message, {'attr': {'class': 'form-control', 'rows': 5}}) }}
                {{ form_errors(form.message) }}
            </div>
           

            <button type="submit" class="btn btn-success w-100">Envoyer</button>
            {{ form_end(form) }}
        {% else %}
            <p>Merci pour votre message ! Nous reviendrons vers vous sous peu.</p>
        {% endif %}
    </div>
</section>
{% endblock %}
