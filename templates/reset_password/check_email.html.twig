{% extends 'base.html.twig' %}

{% block title %}Email de Réinitialisation de Mot de Passe Envoyé{% endblock %}

{% block body %}
    <div class="container mt-5">
        <div class="alert alert-success" role="alert">
            <p>
                Si un compte correspondant à votre adresse email existe, un email contenant un lien de réinitialisation de mot de passe vient d'être envoyé.
                Ce lien expirera dans {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.
            </p>
            <p>
                Si vous ne recevez pas l'email, veuillez vérifier votre dossier de spam ou <a href="{{ path('app_forgot_password_request') }}">essayer à nouveau</a>.
            </p>
        </div>
    </div>
{% endblock %}
